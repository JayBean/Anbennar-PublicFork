# All rebels in province scope
# For siege_won_trigger, siege_won_effect
# FROM = owner
# ROOT = province
# For Other
# FROM = owner
# ROOT = Homecountry
seed_thrall_rebels = {

	color = { 40 183 78 }

	area = nation
	government = theocracy	# Government desired. monarchy/republic/theocracy/anti/any
	religion = song_servants
	defection = none
	defect_delay = 120			# Held provinces will auto-defect after 18 months, regardless of accept/enforce demands.
	independence = none	#What will gain independence (note: ai does not seem to get these rebels if not set to 'none')
	unit_transfer = no			#yes/no | Units switch allegiance after enforcing demands.
	gfx_type = culture_province
	will_relocate = yes			#yes/no | Units will relocate if they have sieged all provinces nearby and has to move over sea.
	
	resilient = yes			#yes/no | Disbands on retreat
	reinforcing = yes			#yes/no | whether the rebels reinforce themselves
	general = yes			#yes/no | whether they have a general
	smart = no			#yes/no | Does, or does not assault forts, tactically withdraw from battle, etc.
	
	artillery = 0.0 	#decimal percentage of army makeup
	infantry = 1.0
	cavalry = 0.0
	
	morale = 1.3

	# Possible handle actions
	handle_action_negotiate = no			#yes/no
	handle_action_stability = no			#yes/no
	handle_action_build_core = no			#yes/no
	handle_action_send_missionary = no			#yes/no	
	
	# The rebel type with the highest modifier for this province gets picked
	spawn_chance = {
		factor = 1
		modifier = {
			factor = 2
			has_country_modifier = effelai_bloom_in_progress
		}
	}
 
 	# This is checked for EACH province in the Area of Operations
	movement_evaluation = {
		factor = 1
		modifier = {
			factor = 0
			OR = {
				NOT = {
					has_province_modifier = effelai_blooming
					has_province_modifier = effelai_blooming_neural_hub
				}
			}
		}
	}
 
 	# Province Scope
 	siege_won_trigger = {

 	}
	siege_won_effect = {
		add_devastation = 25
	}
	
	# Country scope
	can_negotiate_trigger = {	#whether the player is allowed to negotiate

	}
	
	# Country scope
	can_enforce_trigger = {         #any 'demands_enforced_effect' effect will not run unless the trigger evaluates to true
		<trigger>
	}
	
	# Localisation for their demands
	demands_description = "seed_thrall_rebels_demand"
	
	# Country Scope	
	demands_enforced_effect = {
		every_owned_province = {
			limit = {
				OR = {
					has_province_modifier = effelai_blooming
					has_province_modifier = effelai_blooming_neural_hub
				}
			}
			destroy_province = yes
		}
	}
}