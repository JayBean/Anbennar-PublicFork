
country_decisions = {
	
	# find_dark_acolytes = {
		# potential = {
			# #NOT = { has_estate = estate_acolytes }
			# #ruler_has_mage_personality = yes
			
			# #Witch King
		# }
		# allow = {
			# stability = 1
		# }
		# effect = {
			# set_country_flag = estate_acolytes_enabled
				# add_country_modifier = { 
					# name = acolytes_small_cabal 
					# duration = -1 
				# }
		# }
		# ai_will_do = {
			# factor = 0
			# modifier = {
				# factor = 1
				# ruler_has_personality = cruel_personality
			# }
			# modifier = {
				# factor = 1
				# ruler_has_personality = malevolent_personality
			# }
			# modifier = {
				# factor = 1
				# has_heir = no
			# }
		# }
	# }
	ward_spirit_domain = {
		potential = {
			has_country_flag = rending_ward_flag
			is_interacting_with_spirit = yes
		}
		allow = {
			custom_trigger_tooltip = {
				tooltip = spirit_ward_yes_tt
				has_country_flag = rending_ward_flag
			}
			ward_completed = yes
		}
		effect = {
			hidden_effect = {
				
				spirit_clear_interact_flags = yes
			}
		}
		ai_will_do = {
			factor = 10000
		}
	}
}