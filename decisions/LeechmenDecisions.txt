
country_decisions = {

	form_leechdom = {
		major = yes
		potential = {
			
			primary_culture = leechmen
			NOT = { tag = K01 }
			NOT = { has_country_flag = formed_leechdom_flag }
			was_never_end_game_tag_trigger = yes
		}
		provinces_to_highlight = {
			
			AND = {
				region = leechdens_region
				NOT = {
					calc_true_if = {
						amount = 10
						all_province = {
							region = leechdens_region
							owned_by = ROOT
						}
					}
				}
				NOT = { owned_by = ROOT }
			}
			
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			NOT = { 
				OR = 
				{ 
					AND = {
						NOT = {tag = G18}
						exists = G18
					} 
					AND = {
						NOT = {tag = G19}
						exists = G19
					} 
					AND = {
						NOT = {tag = G16}
						exists = G16
					} 
					AND = {
						NOT = {tag = G20}
						exists = G20
					} 
					AND = {
						NOT = {tag = G15}
						exists = G15
					} 
					AND = {
						NOT = {tag = G17}
						exists = G17
					} 
				}
			}
			num_of_owned_provinces_with = {
				value = 10
				region = leechdens_region
			}
		}
		effect = {
			set_country_flag = formed_leechdom_flag
			change_tag = K01
			hidden_effect = { restore_country_name = yes }
			remove_non_electors_emperors_from_empire_effect = yes
			swap_non_generic_missions = yes
			add_prestige = 20
			if = {
				limit = {
					NOT = { government_rank = 2 }
				}
				set_government_rank = 2
			}
			if = {
				limit = { has_custom_ideas = no }
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			leechdens_region = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = ROOT
			}

		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
}