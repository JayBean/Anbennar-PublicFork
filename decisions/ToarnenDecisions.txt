
country_decisions = {
	toarnen_chess_training_mil = {	
		potential = {
			has_country_flag = toarnen_unlock_chess_training
		}
		
		allow = {
			has_mil_advisor_3 = yes
			has_dip_advisor_3 = yes
			custom_trigger_tooltip = {
				tooltip = toarnen_ruler_before_training_tooltip
				NOT = {
					has_ruler_flag = toarnen_ruler_after_training
				}
			}
			mil_power = 150
		}
	
		effect = {
			add_mil_power = -150
			increase_ruler_mil_effect = yes
			set_ruler_flag = toarnen_ruler_after_training 
		}

		ai_will_do = {
			factor = 1
		}
	}

	toarnen_chess_training_dip = {	
		potential = {
			has_country_flag = toarnen_unlock_chess_training
		}
		
		allow = {
			has_mil_advisor_3 = yes
			has_dip_advisor_3 = yes
			custom_trigger_tooltip = {
				tooltip = toarnen_ruler_before_training_tooltip
				NOT = {
					has_ruler_flag = toarnen_ruler_after_training
				}
			}
			dip_power = 150
		}
	
		effect = {
			add_dip_power = -150
			increase_ruler_dip_effect = yes
			set_ruler_flag = toarnen_ruler_after_training 
		}
		
		ai_will_do = {
			factor = 1
		}
	}

	toarnen_host_chess_contest = {	
		potential = {
			has_country_flag = toarnen_unlock_chess_tournament
		}
		
		allow = {
			dip_power = 50
			treasury = 150
			custom_trigger_tooltip = {
				tooltip = toarnen_ruler_before_tournament_tooltip
				NOT = {
					has_ruler_flag = toarnen_ruler_after_tournament
				}
			}
		}
	
		effect = {
			add_dip_power = -50
			add_treasury = -150
			set_ruler_flag = toarnen_ruler_after_tournament
			
			country_event = {
				id = flavor_toarnen.19
				days = 1
			}
		}
		
		ai_will_do = {
			factor = 1
		}
	}
}