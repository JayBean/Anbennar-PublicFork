country_decisions = {
	population_counter_decision = {
		major = yes
		potential = {
			ai = no
			has_country_flag = pop_counter_flag
		}
		
		provinces_to_highlight = {
		}
		
		allow = {
			always = yes
		}
		
		effect = {
			country_event = { id = population.0 }
		}
		
		ai_will_do = {
			factor = 400
			modifier = { factor = 0 NOT = { treasury = 3000 } }
		}
	}
}
