country_decisions = {
	
	mengi_become_angel_tributary = {
		major = yes
		potential = {
			NOT = { tag = L65 }
			NOT = { is_subject_of_type_with_overlord = { who = L65 type = angel_tributary_state } }
			culture_group = mengi
			capital_scope = {
				OR = {
					region = mengi_region
					region = gebahiri_region
				}
			}
		}
		
		allow = {
			exists = L65
			#NOT = { has_ruler_flag = refused_aegis_tributary }
		}
		
		effect = {
			L65 = {
				create_subject = {
					subject_type = angel_tributary_state
					subject = ROOT
				}
			}
		}
		
		ai_will_do = { #The AI can't use this anyway
			factor = 1
		}
	}

}