country_decisions = {

	reclaim_escandar = {
		major = yes
		
		potential = {
			tag = A46
			NOT = { owns = 316 }
			has_country_flag = reclaim_escandar
			NOT = { has_country_flag = escandar_negotiations_started }
		}
		
		allow = {
			treasury = 500
			custom_trigger_tooltip = {
				tooltip = arbaran_no_truce_with_owner_tt
				316 = {
					owner = {
						ROOT = {
							has_truce = PREV
						}
					}
				}
			}
		}
		
		effect = {
			set_country_flag = escandar_negotiations_started
		}
	}
}