#model event for Fey Court Events
country_event = {
	id = fey_court.0
	title = fey_court.0.t
	desc = fey_court.0.d
	picture = FEAST_eventPicture
	goto = fey_prank_seelie
	
	# prevent from firing from MTTH. use if event is triggered only
	is_triggered_only = yes

	# mean time to happen here
	# mean_time_to_happen = {
	# 	days = x
	# }

	trigger = {
		#trigger logic here	
	}
	
	# example of how to modify seelie and unseelie relations
	option = {
		name = fey_court.0.a
		change_variable = { seelieCourtRelations = 1 }
		subtract_variable = { unseelieCourtRelations = 1 }
	}
	option = {
		name = fey_court.0.b
		change_variable = { unseelieCourtRelations = 1 }
		subtract_variable = { seelieCourtRelations = 1 }
	}
}