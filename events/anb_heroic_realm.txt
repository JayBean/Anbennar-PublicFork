namespace = flavour_heroic_realm

country_event = {
    id = flavour_heroic_realm.1
    title = flavour_heroic_realm.1.t
    desc = flavour_heroic_realm.1.d
    picture = 13_Maratha_Estate_eventPicture

	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			random_hired_mercenary_company = {
				limit = {
					is_foreign_company = yes
					hired_for_months = 6
					has_leader = yes
			    }
				save_event_target_as = long_time_hires
		    }
		}
	}

	option = {
		name = flavour_heroic_realm.1.a
		event_target:long_time_hires = { convert_leader_to_general = THIS }
   		add_country_modifier = {
			name = rahen_recruited_general
			duration = 3650
			hidden = yes
		}
	}

	option = {
    	name = flavour_heroic_realm.1.b
        add_prestige = -5
	    }
}

country_event = {
    id = flavour_heroic_realm.2
    title = flavour_heroic_realm.2.t
    desc = flavour_heroic_realm.2.d
    picture = REFORM_eventPicture

    mean_time_to_happen = {
        months = 1800
        modifier = {
            factor = 0.5
            NOT = { stability = 0 }
        }
        modifier = {
            factor = 0.8
            has_any_disaster = yes
        }
    }

    trigger = {
        has_reform = heroic_realm_reform
        NOT = {
            OR = {
                has_country_modifier = two_bags_or_coin
                has_country_modifier = two_bags_and_sword
                has_country_modifier = two_bags_and_gun
            }
        }
    }

    option = {
        name = flavour_heroic_realm.2.a
        ai_chance = {
            factor = 30
        }
        add_treasury = -5
        add_country_modifier = { name = two_bags_or_coin duration = -1 }
    }

    option = {
        name = flavour_heroic_realm.2.b
        ai_chance = {
            factor = 30
        }
        add_country_modifier = { name = two_bags_and_sword duration = -1 }
    }

    option = {
        name = flavour_heroic_realm.2.c
        ai_chance = {
            factor = 40
        }
        add_country_modifier = { name = two_bags_and_gun duration = -1 }
        add_estate_loyalty = {
            estate = estate_adventurers
            loyalty = 10
        }
    }
}